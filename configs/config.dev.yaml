environment: development

server:
  port: 8080
  frontend_url: "http://localhost:5173"

database:
  host: host.docker.internal
  port: 5432
  user: postgres
  dbname: styleai
  sslmode: disable
  auto_create: true